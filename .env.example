# ── CK3FR RP — variables d'environnement ───────────────────────────────
# Copie ce fichier en « .env » et remplis-le avec les clés de ton projet Supabase.
# (Tableau de bord Supabase → Project Settings → API)
#
# ⚠️  Ces deux valeurs sont PUBLIQUES (exposées au navigateur via Vite).
#     La sécurité repose sur la Row Level Security (RLS), pas sur le secret de la clé.
#     Ne mets JAMAIS ici la clé « service_role ».

VITE_SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOi...ta-cle-anon-publique...
